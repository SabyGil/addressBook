import React from 'react';
import AddContact from './AddContact';

const App = (props) => {
  // info = {props.info}
  return (
      <div>
        <AddContact  />
      </div>
    );
}

export default App;
